{
  "name": "universe-analytics-backend",
  "version": "1.0.0",
  "description": "Backend for Universe webhook integration and analytics",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node api/webhook.js",
    "dev": "node --watch api/webhook.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^128.0.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
